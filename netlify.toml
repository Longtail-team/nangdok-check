# 정적 파일 한 장짜리 사이트 - 빌드 과정 없음
[build]
  publish = "."
  command = ""

# 서비스워커/매니페스트는 항상 최신으로 받도록 캐시 최소화
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"

[[headers]]
  for = "/manifest.json"
  [headers.values]
    Cache-Control = "no-cache"
