# 전래동화 출석체크 — 인수인계 / 현재 상태 (2026-07-15)

새 세션에서 이 문서 + 코드(`index.html`, `WebApp.template.gs`)를 보면 이어서 작업 가능.

## 지금 어디까지 됐나 (동작 중)
- 라이브: **https://calm-heliotrope-ed42ed.netlify.app** (레포 `Longtail-team/nangdok-check` → Netlify 자동배포)
- 구조: **Apps Script = 데이터 API / 화면 = Netlify 프론트** (구글 회색 배지 없음, 전체화면, 아이폰 홈아이콘 OK)
- 완료: 설정탭으로 제목·색·개강일 반영 / 자동로그인 / 홈아이콘(🐯) / 자동재시도3회 + 하단 새로고침 안내 / getBootstrap 1회 로딩 / 인스타 수정 자동동기화

### 2026-07-15 추가 반영 (전부 배포됨 — 전래동화 과정)
- **리더보드 이름 = 인증시트 B열(괄호 앞 실명/닉네임)**. 회원이 B열 고치면 반영. @핸들 표기 제거(인스타 아이콘으로 이동). 닉네임=상장 이름 공용.
- **출석 체크 주 단위 오픈**: '오늘까지'→'이번 주(월~금 5일)까지' 클릭·저장. (서버 `submitCheck` weekMax + 프론트 grid `weekMax`)
- **오늘 칸**: 흰 배경 + 앰버 테두리 + 글로우(동작 최소화 기기는 정적 링만).
- **출석 이모지 = 주차별**(매일 순환 폐지). `index.html`의 `WEEK_EMOJIS`(24개) 교체로 과정별 지정, 비우면 기본 💜. 4주=🌞🌛 번갈아, 10주(반쪽이)=🌗. → **새 과정은 이 배열만 교체**.
- **문구 정비**: 제목 '낭독스쿨 {과정명} 출석 페이지'(과정명=설정 B4), '도장'→'출석체크' 통일, 섹션 '오늘 출석'/'출석 체크' 구분, 새로고침 안내(탭 링크), 스플래시 문구 개인화(재방문 시 과정명, localStorage `rd_course`).
- 스플래시 아이콘: 템플릿 기본 📖, 이 과정 🐯(주석 명시).
- ⚠️ 서버 변경(`getLeaderboard`·`submitCheck`)은 라이브 `06 웹앱`에 반영·재배포 완료. **다른 과정 추가 시** 그 과정 Apps Script에도 최신 `06 웹앱` 반영 + 기존 배포 '새 버전'(exec URL 유지) 필요.

## 구성
| 위치 | 내용 |
|------|------|
| `index.html` | 앱 본체(UI+로직), JSONP로 Apps Script API 호출. 맨 위 `const API` = 과정 exec 주소 |
| `manifest.json`·`icons/`·`sw.js` | 홈아이콘/PWA |
| `WebApp.template.gs` | **Apps Script 서버 완성 템플릿** (새 과정마다 재사용) |
| (구글) 인증시트 = TARGET, 원본 신청서 = SOURCE | 데이터 |

## 새 과정 추가하는 법
1. `WebApp.template.gs` 코드를 그 과정 Apps Script 프로젝트에 넣기
2. 원본 **설정 탭**: `B4`=과정명 · `B5`=개강일(날짜) · `B6`=기간(주) · `B7`=색상hex
3. **인증시트 D2** = 체크박스 수(12주→60, 24주→120)
4. 배포 액세스 **"모든 사용자(Anyone)"** → 새 버전 배포
5. `installInstaSyncTrigger` **1회 실행**(인스타 동기화 트리거)
6. 프론트 `index.html`의 `const API`를 그 과정 exec 주소로 (별도 사이트/경로)

## 운영 규칙 (정한 것)
- **가족 = 인스타 계정 기준**. 인증시트에 연락처 없어서 인스타가 유일 열쇠.
- **부모 인스타 하나로 통일**이 기본(효율·안정). 자녀별 개별 인스타는 예외 처리.
- 인스타 바꿀 땐 인증시트에서 고치면 원본시트 자동 반영(트리거). 단 **빈칸→계정**은 자동 안 됨(옛값 없음), **가족 여러 명이면 그 행 전부** 고쳐야 함.
- 한계: 인스타 빈칸 행/서로 다른 인스타 자녀는 로그인에 안 뜰 수 있음(수용).

## 다음 할 일 (미결) — 큰 두 개는 서버 작업이라 다음 세션에서
- [x] ~~리더보드 이름 인증시트 B열~~ → 완료(2026-07-15)
- [ ] **설정 탭 공지 팝업** — 설정탭 셀(예: `B8`=제목, `B9`=내용, 비우면 안 뜸)로 관리자가 공지 팝업 띄우기. 서버 `getConfig`가 셀 읽어 config에 전달 + 프론트 모달 + "다시 보지 않기"(내용 바뀌면 다시 표시). → 서버 재배포 필요.
- [ ] **자녀 상장 발송** — 원본 신청서 **'함께하는 자녀 이름'** 열(쉼표 구분: 예 "낭독핑, 인증핑")로 **자녀별 상장을 부모 이메일로 각각** 발송. 현재 상장코드(`GS 파일/01 출석 & 상장 발송`)는 자녀 열 미사용. 중복방지 키는 `이메일|자녀이름`, 상장 인쇄 이름은 자녀 이름(괄호 앞). 부모도 기존대로 받음.
- [ ] **연락처 기준 가족 그룹핑 마이그레이션(설계 확정)** — 이름·인스타가 둘 다 바뀌어 불안정 → **연락처를 유일 고정 열쇠**로 전환. 신청 1건→인증시트에 사람별 행 '펼치기', 인증시트에 **연락처 열 추가+기존행 백필**, 상장 이메일도 **연락처로 조회**, 웹앱/리더보드 그룹핑도 연락처(→ 인스타 동기화 트리거 불필요). 출석은 사람별 개별 체크 유지. 규모 큼 → **복사본에서 단계별 검증 후 라이브**. (자녀 상장도 이걸 하면 자연 통합)
- [ ] **서브도메인** `attend.nangdokschool.com` 연결 (널리 배포 전에! 홈아이콘에 URL 박힘). DNS 관리처 확인 필요
- [ ] **아이폰 홈화면 추가 안내 하단배너** (iOS 미설치 시에만, 안드로이드 제외)
- [ ] **다른 과정들** 추가 (위 "새 과정 추가하는 법" + 프론트 `WEEK_EMOJIS`·`const API` 교체)
- [ ] (요청 시) **멀티계정 로그인** — 연락처 그룹핑하면 자연 해결
