# 연락처 기반 전환(마이그레이션) 실행 계획서

> 목표: **가족을 묶는 열쇠를 "인스타" → "연락처 해시"로 전환.**
> 인증시트 **A열(고유번호)을 연락처 해시로 재활용**한다. (내비게이션 기능은 웹앱 로그인으로 대체되어 불필요)
> 원칙: **복사본에서 전부 검증 → 무중단 단계 전환.** 앱 안정성 최우선.

작성일 2026-07-15 · 상태: **설계 확정, 미착수**

---

## 1. 핵심 결정 (확정)
- **가족 키 = `연락처 해시`** (전화번호 원본 아님 → 전원 공개 인증시트에 있어도 안전)
- **A열 고유번호를 이 해시로 교체** (새 열·매핑시트·원본시트 수정 전부 불필요)
- **인스타 = 표시 전용**으로 강등 → 각 행에 자유롭게(콤마 입력이든, 자녀 행 직접 작성이든) 넣어도 가족 안 깨짐
- **인스타 동기화 트리거(onEditNangdok) 제거** (더 이상 인스타를 맞출 필요 없음)
- 동명이인: 다른 연락처 → 다른 해시라 자동 분리됨 (문제 없음)

## 2. 구조 Before / After
| 항목 | 지금 (인스타 기준) | 전환 후 (연락처 해시) |
|---|---|---|
| A열(고유번호) | 사람별 순차번호 | **연락처 해시(가족 공유값)** |
| 로그인 | 연락처→원본 인스타→인증시트 인스타 매칭 | 연락처→**해시**→인증시트 A열 매칭 |
| 리더보드 묶기 | 인스타 핸들 | **A열 해시** |
| 인스타 | 열쇠 + 표시 | **표시 전용** |
| 동기화 트리거 | 필요 | **제거** |
| 원본시트 | 로그인 경유 | 로그인에 불필요(생성·상장만 사용) |

## 3. 해시 규격 (Phase 1에서 확정)
- 입력: `연락처 숫자만`(기존 `_digits()`와 동일하게 정규화)
- 함수: `Utilities.computeDigest(SHA_256, SALT + digits)` → hex → **앞 12자리** 사용
- `SALT`: 앱스크립트 안에만 두는 **비밀 문자열**(시트엔 안 보임). 수강생은 해시를 되돌릴 수 없음
- 로그인 시 **동일 함수로 재계산**해 매칭
- 충돌 위험: 12 hex(48비트) → 수천 가족까지 사실상 0

## 4. 단계별 실행 (무중단)
> 핵심: **Phase 2~3에서 해시를 "몰래" 다 채우고 검증한 뒤, Phase 4에서만 로그인을 해시로 전환.** 중간에 로그인 끊기는 순간 없음. 접수는 계속돼도 새 신청은 Phase 2 코드가 자동 처리.

### Phase 0 — 복사본 준비
- [ ] 원본시트 + 인증시트 **복사본** 생성
- [ ] 앱스크립트 프로젝트 **복사본**(또는 테스트 바인딩)에서 작업, 테스트 웹앱이 복사본 데이터를 보게 설정
- [ ] 라이브는 손대지 않음 (전 과정 리허설용)

### Phase 1 — 해시 방식 확정
- [ ] `SALT` 값 결정(랜덤 비밀문자열) · 해시 자릿수(12) 확정
- [ ] 공통 헬퍼 `_familyCode(phone)` 추가 (01·06 양쪽에서 동일 사용)

### Phase 2 — 해시 병행 기록 (로그인은 아직 인스타)
- [ ] **생성 로직 변경**(`01`, `_buildStudentRow`/`_appendNewResponsesLogic`): A열에 순차번호 대신 `_familyCode(원본 연락처)` 기록.
      · 연락처는 원본 행에서 헤더로 찾음(`_sourceCols_`의 연락처 열)
      · 기존의 "마지막 번호 읽어 +1" 로직 제거(더는 증가 안 함)
- [ ] **중복체크 키 변경**: `이름+인스타` → **`이름+해시`** (인스타가 자유로워져도 중복 오판 방지)
- [ ] **백필 스크립트**(1회): 기존 인증시트 각 행 → (현재 인스타로 원본에서 연락처 조회) → 해시 계산 → A열 기록
      · **실행 전 인증시트 A열 스냅샷 백업**(롤백용)
      · 인스타 빈칸/불일치로 연락처 못 찾는 행은 **예외 목록**으로 출력 → 수동 처리
- [ ] 이 시점: 로그인은 **여전히 인스타** → 아무것도 안 깨짐. A열만 해시로 채워짐.

### Phase 3 — 검증
- [ ] 모든 데이터 행(3행~)에 유효한 해시가 있는지
- [ ] 같은 연락처 가족 = 같은 해시 / 다른 연락처 = 다른 해시 확인
- [ ] 실제 가족 몇 팀: `연락처 → 해시 → 그 가족 행들` 정확히 매칭되는지 스팟체크
- [ ] 예외 목록(해시 없는 행) 0건까지 수동 정리

### Phase 4 — 전환 (로그인·리더보드를 해시로)
- [ ] `06 웹앱` 변경:
      · `getFamilyByPhone`: 연락처 → 해시 → A열로 가족 조회(원본·인스타 경유 제거)
      · `_buildFamilyPayload_`: 매칭 기준 인스타 → **A열 해시**
      · `getLeaderboard`: 묶기 기준 인스타 핸들 → **A열 해시** (표시는 행별 인스타 유지)
- [ ] **동기화 트리거 제거**: `onEditNangdok` 및 `installInstaSyncTrigger` 삭제/비활성
- [ ] 복사본에서 전 기능 통과 확인 → 라이브 `06 웹앱`에 반영 → **새 버전 배포**(exec URL 유지)

### Phase 5 — 정리 (선택)
- [ ] A열 해시가 지저분하면 **A열 숨김**
- [ ] 자녀 개별 인스타 방식 도입: ① 신청서 콤마 입력 자동배분 / ② 자녀 행 직접 작성 (둘 다 안전)
- [ ] HANDOFF.md·메모리 업데이트

## 5. 롤백
- 백필 전 **A열 스냅샷** 보관 → 문제 시 A열 복원
- Phase 4 코드는 Apps Script **이전 버전으로 되돌리기** 가능
- Phase 2~3 동안은 로그인이 인스타라, 언제든 중단해도 라이브 정상

## 6. 리스크 & 대응
| 리스크 | 대응 |
|---|---|
| 백필 예외(인스타 빈칸/불일치 행) | 예외 목록 출력 → 수동 채움 (소수 예상) |
| salt 노출 | 앱스크립트 내부에만, 시트/프론트 노출 금지 |
| 순서 사고(먼저 전환) | 반드시 Phase 3 검증 완료 후에만 Phase 4 |
| 접수 중 신규 유입 | Phase 2 생성코드가 새 신청 자동 해시 처리 |

## 7. 실행 시 건드릴 함수 (기술 메모)
- `01 출석 & 상장 발송`: `COLUMN_MAP`(참고), `_appendNewResponsesLogic`(증가로직 제거·해시 기록·중복키), `_buildStudentRow`(A열=해시), + 신규 `_familyCode()`, + 백필 함수
- `06 웹앱`: `getFamilyByPhone`, `_buildFamilyPayload_`(→해시매칭), `getLeaderboard`(→해시그룹), `onEditNangdok`/`installInstaSyncTrigger`(제거), + `_familyCode()` 동일 추가
- 공통 전제: 원본 연락처 열 = 헤더로 탐지(`연락처/전화/휴대`), 정규화는 숫자만
